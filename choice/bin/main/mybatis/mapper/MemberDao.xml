<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper

    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"

    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.study.springboot.dao.IMemberDao"> 


<!-- 회원가입(저장) -->
<insert id="joinDao" > 
		insert into member (user_idx, user_id, user_pw, user_name, user_email) values(member_seq.nextval, #{user_id}, #{user_pw}, #{user_name}, #{user_email})
	</insert>  

<select id="loginDao" resultType="com.study.springboot.dto.MemberDto"> 
      select * from MEMBER Where user_id=? and user_pw=?
</select> 
</mapper> 


